<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Show me the Artist!</title>
  </head>
  <body>
    <h1>Artist Name: <%=@artist.name %></h1><br>
    <h1>Songs:</h1><br>
    <ul>
      <% @artist.songs.each do |song| %>
      <li>
        <a href="/songs/<%= song.slug %>"><%= song.name %></a>
      </li>
      <% end %>
    </ul>
    <h1>Genres:</h1><br>
    <ul>
      <% @artist.genres.each do |genre| %>
      <li>
        <a href="/genres/<%= genre.slug %>"><%= genre.name %></a>
      </li>
      <% end %>
    </ul>
  </body>
</html>
